请先阅读一遍Notes中的事件Event章节内容

WPF路由事件核心要点

层次1：普通事件
   │ 发布者定义事件 → 订阅者订阅 → 触发时通知
   └── 一对一的关系

层次2：路由事件
   │ 发布者定义路由事件 → 多个订阅者订阅 → 事件在视觉树上"旅行"
   └── 一对多的关系，事件会沿着控件树传播

层次3：命令系统
   │ 将事件抽象为命令 → 分离UI和业务逻辑
   └── 更高级的架构模式